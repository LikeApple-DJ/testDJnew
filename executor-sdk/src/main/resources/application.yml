server:
  port: 8081

spring:
  application:
    name: executor-sdk

xxl:
  job:
    admin:
      addresses: http://localhost:8080/xxl-job-admin
    accessToken: default_token
    executor:
      appname: executor-sdk
      port: 9998
      logpath: /data/xxl-job/jobhandler
      logretentiondays: 30

rocketmq:
  name-server: localhost:9876